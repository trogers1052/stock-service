# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=stockservice
DB_SSLMODE=disable

# For migrations (local development)
DB_LOCAL=postgres://postgres:postgres@localhost:5432/stockservice?sslmode=disable

# Kafka Configuration
KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC=stock-events
